

type Perfil {
    id: ID
    nome: String
}

enum usuarioStatus{
    ATIVO
    INATIVO
    BOQUEADO
}


type Usuario {
    id: ID
    nome: String
    idade: Int
    email: String
    telefone: Int
    salario: Float
    vip: Boolean
    bla: String
    perfil: Perfil
    status: usuarioStatus
}